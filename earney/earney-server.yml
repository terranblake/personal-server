apiVersion: apps/v1
kind: Deployment
metadata:
  name: earney-server
  namespace: earney
  labels:
    app: earney-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: earney-server
  template:
    metadata:
      labels:
        app: earney-server
    spec:
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: server
          image: ghcr.io/terranblake/earney-api
          imagePullPolicy: Always
          command: ["cd ./api/src/ && python server.py"]
          ports:
            - name: http
              containerPort: 8001
      volumes:
        - name: config
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: earney-server
  namespace: earney
spec:
  selector:
    app: earney-server
  ports:
    - protocol: TCP
      port: 5001
      name: http
  clusterIP: None
  type: ClusterIP